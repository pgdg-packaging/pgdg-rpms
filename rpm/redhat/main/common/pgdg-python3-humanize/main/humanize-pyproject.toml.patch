--- pyproject.toml.old
+++ pyproject.toml
@@ -1,7 +1,6 @@
 [build-system]
 build-backend = "hatchling.build"
 requires = [
-  "hatch-vcs",
   "hatchling>=1.27",
 ]

@@ -50,10 +49,10 @@
 urls.Source = "https://github.com/python-humanize/humanize"

 [tool.hatch]
-version.source = "vcs"
-version.raw-options.local_scheme = "no-local-version"
+version.source = "path"
+version.path = "src/humanize/_version.py"
+version.pattern = "__version__ = version = ['\"](?P<version>[^'\"]+)['\"]"
 build.artifacts = [ "*.mo" ]
-build.hooks.vcs.version-file = "src/humanize/_version.py"

 [tool.ruff]
 fix = true
