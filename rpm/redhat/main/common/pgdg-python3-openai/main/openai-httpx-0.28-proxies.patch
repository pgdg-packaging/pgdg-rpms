--- src/openai/_base_client.py.old	2026-09-20
+++ src/openai/_base_client.py	2026-09-20 21:57:42.506665476 -0500
@@ -847,7 +847,7 @@
             base_url=base_url,
             # cast to a valid type because mypy doesn't understand our type narrowing
             timeout=cast(Timeout, timeout),
-            proxies=proxies,
+            proxy=proxies if isinstance(proxies, (str, httpx.URL, httpx.Proxy)) else None,
             transport=transport,
             limits=limits,
             follow_redirects=True,
@@ -1432,7 +1432,7 @@
             base_url=base_url,
             # cast to a valid type because mypy doesn't understand our type narrowing
             timeout=cast(Timeout, timeout),
-            proxies=proxies,
+            proxy=proxies if isinstance(proxies, (str, httpx.URL, httpx.Proxy)) else None,
             transport=transport,
             limits=limits,
             follow_redirects=True,
